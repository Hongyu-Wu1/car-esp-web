# 《三轮车 —— 摄像头智能小车》答辩内容（可复制到 PPT）

> 每个大块=一页 PPT，正文是页内要点。视频页注明“演示(循环播放)”。

---

## P1 封面
- 标题：**三轮车 —— 摄像头智能小车**
- 副标题：视觉 + 离载架构，实现 循迹 / 避障 / 停车 / 自动双球入洞
- 底座：基于 ESP32-S3 + USB 摄像头 + 笔记本端算法
- 组别：**第 16 组**
- 组员：沙也涵 · 王翊泽 · 吴泓谕
- 应用场景：摄像头智能循迹小车 / 竞赛任务
- （右侧配图：小车实物示意）

---

## P2 目录 · 三个创新点
- **① 项目管理**：Git & GitHub 分支/提交管理，远端备份
- **② 架构**：ESP 采集执行 + 笔记本计算 & 实时显示，C / py 分层
- **③ 算法**：巡线 / 避障 / 推球 —— 自适应阈值 + 多反馈 + 动态 HSV

---

## P3 创新① 项目管理（Git & GitHub）
- **为什么用 Git & GitHub**
  - 多人无法同时编辑同一份代码
  - 去年 AI 一条 `rm` 把整个项目删了，本地没备份 → 没有远端保存就全没了（教训）
- **做法**
  - 用 分支 + 提交 + 推送 到 GitHub 远端备份
  - 每项功能开一个 feature 分支
  - 验证通过 merge 回 main
  - 文档随代码一起提交
  - 提交信息规范（功能 / 修复 / 重构…）
- **分支（9 条）**：feat-follow-line-and-kick-ball(当前) · feat-camera-line-follow · feat/line-follow · feat/no-wifi-speed-feedback · docs/pin-table · simple-version · tested/car-esp-newest · weekone · main
- **提交图（近期）**：

```
* 38f93a7  功能: 拆分靠近/击球油门
* d5cd024  修复: 桥接遥测修复油门狂飙
* 9e56bbb  重构: 感知/网络/任务协调 三层
* 5fc69d4  修复: 卡住提k 上限
* 573d4a9  功能: 推球接 anti_stall
* 4e090f2  功能: 吸收推球入洞(HSV 调色)
* 53eaccb  文档: main 并入 feat 最新
```

---

## P4 创新② 架构（ESP 采集执行 + 笔记本计算显示）
- **整体数据流**
  - `ESP32-S3`（采集 & 执行：摄像头 / 超声 / 电机 / 舵机）→ `WiFi`(softAP+TCP：原始 MJPEG 流 + 遥测) → `笔记本`（计算 & 实时显示：巡线 / 避障 / 推球 / 录像）→ 回控 `MOTOR / SERVO`
- **遇到的主要问题**
  - 反复烧录 / 串口不好读 → 改算法不用烧固件，报错在哪屏上一眼看到
  - 夜晚没真机也能调试（录播 + test&sim 仿真验证再上机）
- **最后一个提交的文件关系树**

```
main/   (C 固件)
  app/     remote_b.c · main.c
  comm/    wifi_link.c
  motion/  motor_driver.c · servo.c
  sensing/ camera.c · ultrasonic.c · wheel_encoder.c
  display/ dashboard.c · tft.c

py/     (笔记本 · 算法)
  main.py            编排 / IO
  wifi_link.py       网络(收包/重连/发送)
  perception.py      帧 → 观测
  task_controller.py 协调(FOLLOW→RAISE→PUSH)
  record.py          展示 / 录像 / CSV
  actions.py · vision.py · mode*.py
  config.py · ball_vision.py · color_tune.py
```

---

## P5 创新③ 算法 · 巡线（种子连通域 + Otsu 自适应阈值）
- **黑色线判定**：Otsu 自适应二值化
  - 阈值用『种子框』(车底窄带)算，抗光照
  - 种子框对比度低 → 判丢线，不进 Otsu
- **判向**：与种子框连通的线块 → 质心偏置
  - `offset` 位置误差 → 连续 P 控制
  - `bias` 整块质心 → 方向记忆(丢线按记忆转)
- **问题**：光照 / 地面干净程度不固定 → Otsu 自适应(不用固定阈值)，对比度兜底
- **演示(循环播放)**：巡线 —— 第一视角·带标注画面

---

## P6 创新③ 算法 · 避障（左移绕板 · 双反馈）
- **流程**：左移绕板 → 直行定距 → 右移找线回线；板 = 障碍，绕行而非停
- **双反馈调节**(左移时同时闭环)
  - ① 让『板/地分界线』保持水平(倾角当误差)
  - ② 超声距离反馈：维持与板 ~10cm(太近后退 / 太远前进)
- **问题**：每次 WiFi 延迟不确定，到板距离不好测 → 保证『退出左移时距离固定』 → 后面『直行定距』才能硬编码时长
- **演示(循环播放)**：左移避障 —— 第一视角·带标注画面

---

## P7 创新③ 算法 · 推球（找球 · 动态 HSV 调色）
- **识别**：红球 / 绿球 + 蓝色洞口
  - 用 HSV 而非 RGB(颜色/亮度分离，抗光照)
  - Hue 按环形算(0/180 相接) → 红色跨边界也能框住
  - 球过滤：圆度 / 面积 / 宽高比；洞口只看颜色面积
- **动态 HSV(实时调色台)**
  - 三槽(球1 / 球2 / 洞)各 6 滑块 → 拖动即时生效
  - R 还原默认，S 打印当前值 → 粘回 config 固化
  - 状态机：搜索 → 靠近 → 对球 → 对洞 → 横移 → 击球
- **演示(循环播放)**：击球 —— 第一视角·带标注画面

---

## P8 总结 · 收获（Q&A）
- **主要收获**
  - 嵌入式 × CV × 控制的完整闭环
  - 架构分层：感知 / 控制 / 状态机 / 编排 各司其职
  - 用 git 管理多人在线协作 + 远端备份
  - 真机踩坑 → 定位 → 修复(油门狂飙 / 舵机线松 / 掉线)
  - 仿真 / 录播先验证，再上真机
- **一句话总结**：*『算法全部离载到笔记本，改逻辑不用烧固件；摄像头视觉 + 多状态机，从循迹一路做到自动双球入洞』*
- **谢谢 · 欢迎提问**
- 第 16 组 · 沙也涵 / 王翊泽 / 吴泓谕
